struct sk_buff
{
// 所有的sk_buff 都由一个双向链表，将所有的sk_buff穿起来
struct sk_buff *next;
struct sk_buff *prev;

struct sk_buff_head *list; // 指向链表头


}

// 链表头， 大小很小
struct sk_buff_head
{
struct sk_buff *next;
struct sk_buff *prev;
__u32 qlen; // 链表的长度
spinlock_t lock; // 防止对表并发访问
}

function:

